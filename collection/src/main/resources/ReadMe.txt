List接口：继承了Collection接口
    /----List:存储有序的，可重复的数据
         /----ArrayList:线程不安全的，底层为Object数组。当按照顺序插入数据和根据索引读取数据的时候效率比较高。
                构造方法：
         /----LinekdList：线程不安全的，底层为双向链表结构，具体为Node内部类。当修改数组和插入数据的效率比较高，读取速度慢，时间复杂度为O(n)
              /----底层原理：
                    (1) 当插入数据时，先计算插入数据的对象的hashcode，如果链表中不存在
         /----Vector
    /----Set

Map接口：继承了Collection接口